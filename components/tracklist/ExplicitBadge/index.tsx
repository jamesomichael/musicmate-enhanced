import React from 'react';

const ExplicitBadge = () => {
	return (
		<div className="mr-1 flex justify-center items-center h-3.5 md:h-4 aspect-square bg-neutral-300 rounded-xs">
			<span className="font-bold text-[0.6rem] md:text-[0.7rem] text-spotify-black">
				E
			</span>
		</div>
	);
};

export default ExplicitBadge;
